<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="4dp"
    android:paddingEnd="4dp">

    <!-- 기존 LinearLayout (요일 라벨 + 캘린더) -->
    <LinearLayout
        android:id="@+id/calendar_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 왼쪽 요일 라벨 -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:text="Mon"
                android:textColor="@color/white"
                android:alpha="0.8" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:text="Wed"
                android:textColor="@color/white"
                android:alpha="0.8" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:text="Fri"
                android:textColor="@color/white"
                android:alpha="0.8" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:text="Sun"
                android:textColor="@color/white"
                android:alpha="0.8" />
        </LinearLayout>

        <!-- 오른쪽 기여도 캘린더 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/calendar_recycler_view"
            android:layout_width="700dp"
            android:layout_height="wrap_content"
            android:padding="4dp" />



    </LinearLayout>

    <!-- 새로운 LinearLayout (RecyclerView 아래 텍스트 3개) -->
    <LinearLayout
        android:id="@+id/bottom_texts_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="left"
        android:paddingStart="48dp"
        app:layout_constraintTop_toBottomOf="@id/calendar_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/text_label_1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="최근 1년간 재생횟수:"
            android:textColor="@color/white"
            android:paddingEnd="12dp" />

        <TextView
            android:id="@+id/played_count_last_year"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="#"
            android:textColor="@color/white"
            android:paddingEnd="12dp" />

        <TextView
            android:id="@+id/selected_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2025-07-30"
            android:textColor="@color/white"
            android:paddingEnd="12dp" />

        <TextView
            android:id="@+id/played_count_for_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="#"
            android:textColor="@color/white"
            android:paddingEnd="12dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
